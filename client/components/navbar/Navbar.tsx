import React from 'react'
import NavLinks from './NavLinks'

const Navbar = () => {
    return (
        <nav className = "main-navbar">
            <NavLinks />
        </nav>
    )
}

export default Navbar